# AVNU paymaster key (https://portal.avnu.fi) — sponsors gas on private swaps.
# Copy to .env.local and fill in. NEVER commit .env.local.
# NOTE: values bundled into a browser app are publicly readable. For production,
# proxy the paymaster call through your own backend instead.
VITE_AVNU_PAYMASTER_API_KEY=
